.comment
  .username
    = comment.user.username
  .comment-content
    = comment.content
  - if comment.user == current_user
    = link_to riff_comment_path(riff, comment), method: :delete, data: { confirm: "Are you sure?" }, remote: true do
      %span.glyphicon.glyphicon-remove.delete-comment
